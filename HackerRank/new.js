let hi = [1, 2];
let number = [1, 3, 5, 5, 3, 53, 5, 3, 5, 35, 35, 35, 5, 4, 3, 2, 5, 3, , 5, 3];

function designerPdfViewer(h, word) {
  let ab = [];
  for (let i = 0; i <= word; i++) {
    if (h === typeof number) ab++;
    let max = Math.max(...ab);
    console.log(max);
  }
}
designerPdfViewer(hi, number);
